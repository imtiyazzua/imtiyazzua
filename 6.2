/***********************************************************/
/*Program to find the largest of two numbers */
/* Demonstrating the use of relitonal operators */
/***********************************************************/
#include<stdio.h>
main()
{
float a,b,c,max;
printf("Enter a,b,c:\n");
sanf("%f%f%f",&a,&b,&c);
/*Compute the largest of three numbers */
/*-------------------------------------------------------------------*/
max=a;
if(b>max)max=b;
if(c>max)max=c;
printf("Largest of the three numbers = %7.2f\n",max);
}
